
---

# MTP-manager

Универсальный менеджер для работы с MTProto-прокси на Linux-серверах. Позволяет быстро устанавливать, запускать и обновлять MTProto-прокси через простой bash-скрипт.

---

## 🔹 Файлы

* **`mtproto-universal.sh`** — основной скрипт установки и управления MTProto-прокси.
* **README.md** — документация по использованию.

---

## 🔹 Требования

* Linux (Debian/Ubuntu/CentOS)
* Bash ≥ 4.0
* curl или wget

---

## 🔹 Установка и запуск

> **Важно:** скачивать только через **raw URL**, иначе скрипт загрузит HTML-страницу и появится ошибка:
>
> ```bash
> syntax error near unexpected token 'newline'
> ```

### 1. Скачивание скрипта

С использованием `curl`:

```bash
curl -fsSL https://raw.githubusercontent.com/tarpy-socdev/MTP-manager/refs/heads/main/mtproto-universal.sh -o mtproto-universal.sh
chmod +x mtproto-universal.sh
```

Или с `wget`:

```bash
wget -O mtproto-universal.sh https://raw.githubusercontent.com/tarpy-socdev/MTP-manager/refs/heads/main/mtproto-universal.sh
chmod +x mtproto-universal.sh
```

---

### 2. Проверка синтаксиса (рекомендуется перед запуском)

```bash
bash -n mtproto-universal.sh
```

Если ошибок нет, можно запускать.

---

### 3. Запуск

```bash
sudo ./mtproto-universal.sh
```

Следуй инструкциям скрипта для установки и конфигурации прокси.

---

## 🔹 Поддержка

Если возникают проблемы:

* Проверяй первую строку скрипта: она должна быть `#!/bin/bash`.
* Никогда не скачивай через `github.com/.../blob/...` — только через `raw.githubusercontent.com`.
* Ошибки с синтаксисом почти всегда связаны с HTML вместо bash-скрипта.

---

## 🔹 Лицензия

MIT License © 2026

---


